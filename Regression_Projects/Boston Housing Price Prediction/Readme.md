# Boston House Price Prediction

This project is a regression-based project in which multiple features (inputs) are used to predict the output (target value) for the house price. The model learns from the given data using supervised learning techniques to make accurate predictions.

---
